name: Run production batch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/admin-request.yml
    with:
      operation: run-batch
    secrets: inherit
