name: Sync Creative West source only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    uses: ./.github/workflows/admin-request.yml
    with:
      operation: sync-creative-west
    secrets: inherit
