name: Inspect Notion review queue

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    uses: ./.github/workflows/admin-request.yml
    with:
      operation: inspect-notion-review
    secrets: inherit
